import com.epam.jwt.task2.reader.impl.DataReaderImpl;
import org.testng.Assert;

public class Test extends Assert {

    @org.testng.annotations.Test
    public void test(){
       // assertEquals(3, new DataReaderImpl("resources/file.txt").readData().size());
    }
}
